{
  "name": "mediapipe-parity-web",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "description": "Parity reference: runs MediaPipe Tasks-Vision (Hand/Pose/Face) in the browser and exports JSON.",
  "scripts": {
    "setup": "bash setup_assets.sh",
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@mediapipe/tasks-vision": "0.10.18"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vite": "^5.4.0"
  }
}
